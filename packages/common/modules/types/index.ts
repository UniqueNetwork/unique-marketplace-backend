export enum OfferStatus {
  Opened = 'Opened',
  Canceled = 'Canceled',
  Completed = 'Completed',
}

export enum OfferEventType {
  Open = 'Open',
  Revoke = 'Revoke',
  Cancel = 'Cancel',
  Buy = 'Buy',
}
